# Security agent API base URL.
# For local development use the backend origin, e.g. http://localhost:8000.
# If omitted, browser requests use current origin and SSR falls back to localhost:8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Next.js Output Mode (standalone for Docker)
NEXT_OUTPUT=standalone

